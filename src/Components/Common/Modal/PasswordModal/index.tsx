import Modal from '@/Components/Common/Modal';

const PasswordModal = ({ onChangeOpen }) => {
  return (
    <Modal
      height={20}
      onChangeOpen={onChangeOpen}
    >
      alarm
    </Modal>
  );
};

export default PasswordModal;
